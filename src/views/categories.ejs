<%- include('partials/header') %>
<main>
  <h1><%= title %></h1>

  <p>Here are some of our service project categories:</p>

  <ul>
    <% categories.forEach(category => { %>
      <li><%= category.name %></li>
    <% }); %>
  </ul>
</main>
<%- include('partials/footer') %>